# lkm = Lasso.js + Koa + Marko, the expressive starter kit for the LKM open source libs.

To enjoy this project, simply run `npm install && npm run dev`.
Production mode => `NODE_ENV=production node server.js`
